appId: com.airwallex.paymentacceptance
tags:
  - schema
  - embedded
  - guest
  - session
platform:
  android:
    disableAnimations: true  # Disables system animations on the Android emulator
---
- runFlow:
    when:
      true: ${IS_RECORDING}
    commands:
      - startRecording: test_embedded_element_accordion_schema_session
- runScript: ../../Common/settings.js
- runFlow:
    file: ../../Card/flow_embedded_element_schema_payment.yaml
    env:
      LAYOUT: ${output.settings.layout.accordion}
      USE_NEW_SESSION: true
- runFlow:
    when:
      true: ${IS_RECORDING}
    commands:
      - stopRecording
