appId: com.airwallex.paymentacceptance
tags:
  - card
  - embedded
  - guest
platform:
  android:
    disableAnimations: true  # Disables system animations on the Android emulator
---
- runFlow:
    when:
      true: ${IS_RECORDING}
    commands:
      - startRecording: test_embedded_element_accordion_card
- runScript: cards.js
- runScript: ../Common/settings.js
- runFlow:
    file: flow_embedded_element_accordion_payment.yaml
    env:
      LAYOUT: ${output.settings.layout.accordion} # Use the accordion layout for this test
      CARD_NUMBER: ${output.cards.visa} # Card number for testing
- runFlow:
    when:
      true: ${IS_RECORDING}
    commands:
      - stopRecording
